// function addShipping(price: number, shipping: number) {
//     let total = price + shipping;
//     console.log(`$${total}`);
// }
// addShipping(10, 2);
var items = Array();
items = ['pancake', 'socks', 'ketchup'];
for (var _i = 0, items_1 = items; _i < items_1.length; _i++) {
    var item = items_1[_i];
    console.log(item);
}
